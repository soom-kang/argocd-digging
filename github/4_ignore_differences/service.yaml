apiVersion: v1
kind: Service
metadata:
  name: ignore-diff-demo
  namespace: study-ignore-diff
spec:
  selector:
    app: ignore-diff-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
