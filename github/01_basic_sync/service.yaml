apiVersion: v1
kind: Service
metadata:
  name: basic-sync-nginx
  namespace: study-basic-sync
spec:
  selector:
    app: basic-sync-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
