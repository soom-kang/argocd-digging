apiVersion: apps/v1
kind: Deployment
metadata:
  name: team-b-nginx
  namespace: study-appset-team-b
  labels:
    app: team-b-nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: team-b-nginx
  template:
    metadata:
      labels:
        app: team-b-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:1.26-alpine
          ports:
            - containerPort: 80
